Int[][] x = streams
if(x.length == 0){
    return(x)
}else{
    Int[] stream1 
    stream1 = x.getElement(0)
    Int[] stream2 
    stream2.push(0)
    Int index = 0
    while(index < stream1.length){
        stream2.push(stream1.getElement(index))
        index=index+1
    }
    x.push(stream2)
    return(x)
}